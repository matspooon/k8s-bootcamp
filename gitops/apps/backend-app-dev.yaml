apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-app-dev
  namespace: dev-tools # argocd가 설치된 네임스페이스
spec:
  project: default
  source:
    #repoURL: https://github.com/your-org/gitops-repo.git
    repoURL: http://gitea-http.dev-tools.svc.cluster.local:3000/admin/manifest-repo.git
    path: charts/backend-app
    targetRevision: main
    helm:
      valueFiles:
        - ../../envs/dev/backend-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: apps-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  # ignoreDifferences:
  #   - group: apps
  #     kind: Deployment
  #     jqPathExpressions:
  #       - .spec.template.spec.containers[].livenessProbe
  #       - .spec.template.spec.containers[].readinessProbe
  #       - .spec.template.spec.containers[].startupProbe
  #       - .spec.template.spec.containers[].resources